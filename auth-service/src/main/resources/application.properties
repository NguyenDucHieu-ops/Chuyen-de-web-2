server:
  port: 8816

spring:
  application:
    name: auth-service
  datasource:
    # Điền đúng url, user, pass của DB máy em vào đây nhé
    url: jdbc:mysql://localhost:3306/rainbow_erp_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/